osx_image: xcode8.1
language: objective-c
cache: cocoapods
before_install:
- gem install cocoapods 
script:
- set -o pipefail && xcodebuild clean build build-for-testing -project yalu102.xcodeproj -configuration Debug | xcpretty
