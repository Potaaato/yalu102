osx_image: xcode8.1
language: objective-c
cache: cocoapods
before_install:
- gem install cocoapods 
script:
- set -o pipefail && xcodebuild clean build build-for-testing -project Example/yalu102.xcodeproj -scheme yalu102 -configuration Debug | xcpretty
